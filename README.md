One folder is an Analysis folder, this folder contains a text file that contains my analysis of the assignment
One folder is PyBank, This contains a python file that tracks and manipulates the data, the second file is a text file of the results
One folder is PyPoll, This contains a python file that tracks and manipulates the data, the second file is a text file of the results
One folder is Starter_Code, this folder contains the csv files that we are working with
